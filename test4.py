a = int(input("Enter the first number: "))
b = int(input("Enter the second number: "))

try:
    c = a / b
    print(c))
    except:
    print("syntsx error")

# we are not getting the syntsx error message and not the output
# just we got unmatched ')'
# because this is compile time error
# we can not handle syntax error with the help of try and except